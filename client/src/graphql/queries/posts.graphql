query Posts($limit: Int!, $cursor: String) {
  posts(limit: $limit, cursor: $cursor) {
    totalCount
    cursor
    hasMore
    PaginatedPosts {
      id
      title
      text
      createdAt
      updatedAt
      textSnippet
      user {
        username
      }
    }
  }
}
